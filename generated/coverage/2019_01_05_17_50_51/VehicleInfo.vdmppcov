class VehicleInfo
instance variables
  Seats:int;
  Doors:int;
  Model:seq of (char);
  Year:int;
  FuelType:seq of (char);
  Type:seq of (char);
  Brand:seq of (char);
  Color:seq of (char);

end VehicleInfo
